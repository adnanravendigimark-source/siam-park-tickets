node_modules
.next
out
.env
.env*.local
.DS_Store
*.tsbuildinfo

# Images uploaded through the admin CMS (kept locally, not committed)
public/uploads

# Admin user accounts (password hashes) — created at runtime, not sample data
data/users.json
